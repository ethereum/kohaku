- [x] Convert state into tabular state
- [ ] Make RailgunProvider builder pattern
- [ ] Add sensible defaults for prover, artifact loader, utxo indexer, utxo verifier, txid indexer, txid verifier, and bundler.
  - To do this will need an RPC URL and a bundler URL.  Can probably have::new add the RPC provider, ::with_poi add the txid stuff, and ::with_bundler add the bundler.
- [ ] Audit public API for consistency and minimalism
  - Consider:
    - Do we want the typescript to just expose the RailgunProvider, or expose all the public rust functionality?  Probably the former for a minimal API surface, and can expose more if needed.
    - Do we want people to use our BundlerProvider, or to use something from viem?  Probably the latter for now for a more minimal, API surface but we can definitely add BundlerProvider if it increases complexity at all.
      - Main issue with adding it is ensuring all the UserOp* types have tsify definitions.
- [ ] Add docstrings to public API
- [ ] Clean up integration tests to all use the `anvil` / `alto` rust fixtures
- [ ] Add integration test for plugin interface.

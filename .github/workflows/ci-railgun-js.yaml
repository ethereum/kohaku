name: CI Railgun JS

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  ci-railgun-js:
    runs-on: ubuntu-latest
    env:
      RPC_URL_MAINNET: ${{ secrets.RPC_URL_MAINNET }}
      RPC_URL_SEPOLIA: ${{ secrets.RPC_URL_SEPOLIA }}
      DEV_KEY: ${{ secrets.DEV_KEY }}
      DEV_ADDRESS: ${{ secrets.DEV_ADDRESS }}
      DEV_VIEWING_KEY: ${{ secrets.DEV_VIEWING_KEY }}
      DEV_SPENDING_KEY: ${{ secrets.DEV_SPENDING_KEY }}
    steps:
    - uses: actions/checkout@v5

    # - uses: ./.github/actions/load-sops-secrets
    #   with:
    #     age_key: ${{ secrets.SOPS_AGE_KEY }}

    - uses: ./.github/actions/setup-workspace
      with:
        shell: ci-js
        build-packages: false

    - run: cd packages/railgun && pnpm install

    - name: Pack wasm
      run: cd packages/railgun && just wasm

    - name: Run sync-utxo
      run: cd packages/railgun && just integration-sync-utxo

    - name: Run transact
      run: cd packages/railgun && just integration-transact

    # - name: Run transact-poi
    #   run: cd packages/railgun && just integration-transact-poi

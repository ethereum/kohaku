---
"@kohaku-eth/social-recovery": patch
---

Add the passkey method, `passkeyMethod()`: WebAuthn creation and request options under the integrator's relying-party id, the P-256 point and relying-party hash as config, the assertion with a low `s` as proof, and the module's check list as the local verdict.

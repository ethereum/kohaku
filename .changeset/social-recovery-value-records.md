---
"@kohaku-eth/social-recovery": patch
---

Replace the placeholder value records with the SDK's real records, closed sets and build constants (the digest version, the body encoder key, the method `verify` ABI and its magic value).

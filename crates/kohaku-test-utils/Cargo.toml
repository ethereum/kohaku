[package]
name = "kohaku-test-utils"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
crate-type = ["rlib"]

[dependencies]
alloy = { workspace = true, features = ["provider-anvil-api", "node-bindings"] }
tokio = { workspace = true, features = ["full"] }
tracing = { workspace = true }
userop-kit = { workspace = true }

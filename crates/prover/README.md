# Prover

This crate provides a generic prover interface for generating circuit proofs. It's designed
to be used with different proving systems, providing a common API for proof generation. It also includes
a TypeScript interface and wrapper for ease of use in JavaScript/TypeScript environments.
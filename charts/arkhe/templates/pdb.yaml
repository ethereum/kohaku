apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "arkhe.fullname" . }}
  labels:
    {{- include "arkhe.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "arkhe.selectorLabels" . | nindent 6 }}

# @kohaku-eth/railgun

Placeholder package so `@kohaku-eth/railgun` stays in the pnpm workspace (and changesets keep working) while the real implementation is reintroduced.

Not for production use. Do not depend on this stub.

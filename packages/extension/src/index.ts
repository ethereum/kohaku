import { accountVersion } from '@ethprivacy/account';
import { nodeVersion } from '@ethprivacy/node';
import { txVersion } from '@ethprivacy/tx';

export const extensionInfo = {
  accountVersion,
  nodeVersion,
  txVersion
};
export const hello = 'foo';
